// @ledgeriq/shared scaffold.
// Slice 1 PR (b) ports this package to TypeScript and adds the zod request schemas,
// money helpers (integer minor units; JSON wire format is a number below 2^53), and
// timezone-aware date helpers described in docs/ledger.md.
export const SHARED_PACKAGE = "@ledgeriq/shared";
